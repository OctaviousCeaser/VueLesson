// function needed to get random values
function getRandomValue(min, max) {
    // this equation give us a value between the min and max value 
    return Math.floor(Math.random() * (max - min)) + min;
}
